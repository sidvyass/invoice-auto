[project]
name = "invoice-generator"
version = "0.1.0"
description = "Air ticket invoice PDF generator"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0,<2",
    "reportlab==4.4.9",
    "streamlit>=1.42,<2",
]

[tool.uv]
package = false
